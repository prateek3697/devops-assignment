document.addEventListener("DOMContentLoaded", () => {
    const contentDiv = document.getElementById("content");
    contentDiv.innerText = "Hello, this is a simple JavaScript application served by NGINX!";
});